# The config.py file under the config/ directory typically contains configurations for your application. 
# These configurations can include settings such as database connection details, API keys, file paths, logging settings, 
# feature flags, and other parameters that your application might need.

#example
# Database configuration
DB_HOST = 'localhost'
DB_PORT = 5432
DB_NAME = 'my_database'
DB_USER = 'my_user'
DB_PASSWORD = 'my_password'

# API keys
API_KEY = 'my_api_key'

# File paths
DATA_DIR = '/path/to/data/directory'
LOG_DIR = '/path/to/log/directory'

# Logging settings
LOG_LEVEL = 'INFO'

# Feature flags
FEATURE_FLAG_ENABLED = True